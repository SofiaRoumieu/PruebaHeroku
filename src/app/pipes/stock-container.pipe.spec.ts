import { StockContainerPipe } from './stock-container.pipe';

describe('StockContainerPipe', () => {
  it('create an instance', () => {
    const pipe = new StockContainerPipe();
    expect(pipe).toBeTruthy();
  });
});
